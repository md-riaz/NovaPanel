<?php
/**
 * NovaPanel Environment Configuration Example
 * 
 * Copy this file to .env.php and update the values with your actual credentials.
 * 
 * Command: cp .env.php.example .env.php
 * 
 * IMPORTANT: Never commit .env.php to version control as it contains sensitive credentials.
 * 
 * NOTE: NovaPanel uses SQLite for all panel operations (users, sites, etc.)
 *       MySQL/PostgreSQL credentials below are ONLY for creating customer databases
 */

// MySQL Root Credentials (for creating CUSTOMER databases ONLY)
// The panel itself uses SQLite, NOT MySQL
// These credentials are used when panel users request MySQL databases for their websites
putenv('MYSQL_HOST=localhost');
putenv('MYSQL_ROOT_USER=root');
putenv('MYSQL_ROOT_PASSWORD=your_mysql_root_password_here');

// PostgreSQL Root Credentials (optional, for creating CUSTOMER databases ONLY)
// The panel itself uses SQLite, NOT PostgreSQL
// Uncomment these lines if you plan to offer PostgreSQL databases to panel users for their websites
// putenv('PGSQL_HOST=localhost');
// putenv('PGSQL_ROOT_USER=postgres');
// putenv('PGSQL_ROOT_PASSWORD=your_pgsql_root_password_here');

// PowerDNS Database Credentials (for DNS management)
// These credentials are used to manage DNS records in PowerDNS
putenv('POWERDNS_HOST=localhost');
putenv('POWERDNS_DATABASE=powerdns');
putenv('POWERDNS_USER=powerdns');
putenv('POWERDNS_PASSWORD=your_powerdns_password_here');

// Application Settings
putenv('APP_ENV=production');        // Environment: production, development, or testing
putenv('APP_DEBUG=false');           // Set to 'true' only for debugging (not recommended in production)
putenv('APP_URL=http://your-server-ip:7080');  // Panel URL

/**
 * INSTALLATION NOTES:
 * 
 * 1. Copy this file to .env.php:
 *    cp .env.php.example .env.php
 * 
 * 2. Update all password placeholders with your actual credentials
 * 
 * 3. Set correct file permissions (readable only by panel user):
 *    chmod 600 .env.php
 *    chown novapanel:novapanel .env.php
 * 
 * 4. Verify the configuration is loaded by accessing the panel
 * 
 * SECURITY:
 * - Keep this file secure and never share it publicly
 * - Use strong passwords for all services
 * - Regularly rotate credentials
 * - Ensure file permissions are restrictive (600)
 */
